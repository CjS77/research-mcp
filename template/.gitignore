__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Backup-setup config written by `research-kb backup init` (rclone remote name, local dir).
# Environment-specific; the crypt passphrase itself lives in the OS keychain, never here.
/work/backup.env

# KB index can exceed GitHub's 100 MB per-file limit.
# Rebuild with `research-kb init && research-kb index --scan reference`, or fetch it (see README).
/work/data/
*.sqlite
*.sqlite-shm
*.sqlite-wal
